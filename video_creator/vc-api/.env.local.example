# vc-api 로컬 실행용 — 이 파일을 .env.local로 복사 후 값 채우기
# cp .env.local.example .env.local

PORT=5000

# MariaDB — 아래 두 옵션 중 하나 선택
# 옵션 A: EC2 DB 직접 연결 (로컬 MySQL 없을 때)
DB_HOST=3.34.99.69
DB_PORT=3306
DB_USER=aiadmin
DB_PASSWORD=our10680^^
DB_NAME=ai_proposal

# 옵션 B: 로컬 MySQL
# DB_HOST=localhost
# DB_USER=root
# DB_PASSWORD=your_password

# 파일 저장 경로
UPLOAD_BASE_PATH=./uploads
SERVER_BASE_URL=http://localhost:5000

# Claude AI
ANTHROPIC_API_KEY=

# fal.ai — 비워두면 Mock 모드 자동 전환
FAL_KEY=
FAL_MODEL=fal-ai/kling-video/v1/pro
